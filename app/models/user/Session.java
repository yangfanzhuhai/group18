package models.user;

public class Session {

  // TODO: Currently a shell model of a session between the server and the
  // client. This must be a static session (only store authentication details)
  // to maintain REST criteria.
  
}
